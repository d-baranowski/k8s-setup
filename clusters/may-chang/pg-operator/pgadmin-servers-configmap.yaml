apiVersion: v1
    }
      }
        }
          "Comment": "CloudNativePG db-cluster - postgres superuser"
          "PassFile": "/pgpass/.pgpass",
          "SSLMode": "prefer",
          "Username": "postgres",
          "MaintenanceDB": "postgres",
          "Port": 5432,
          "Host": "db-cluster-rw.default.svc.cluster.local",
          "Group": "May-Chang Cluster",
          "Name": "db-cluster (postgres superuser)",
        "2": {
        },
          "Comment": "CloudNativePG db-cluster - owner user"
          "PassFile": "/pgpass/.pgpass",
          "SSLMode": "prefer",
          "Username": "owner",
          "MaintenanceDB": "postgres",
          "Port": 5432,
          "Host": "db-cluster-rw.default.svc.cluster.local",
          "Group": "May-Chang Cluster",
          "Name": "db-cluster (CNPG)",
        "1": {
      "Servers": {
    {
  servers.json: |
data:
  namespace: default
  name: pgadmin-servers-config
metadata:
kind: ConfigMap
