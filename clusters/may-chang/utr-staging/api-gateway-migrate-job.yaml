# The API gateway keeps its rate-limit buckets in its own schema. Without this
# job the limiter cannot find its table and fails open on an internet-facing
# host.
apiVersion: batch/v1
kind: Job
metadata:
  name: utr-staging-api-gateway-migrate
  namespace: default
  labels:
    app: utr-staging-api-gateway-migrate
  annotations:
    kustomize.toolkit.fluxcd.io/force: enabled
spec:
  ttlSecondsAfterFinished: 86400
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: utr-staging-api-gateway-migrate
    spec:
      restartPolicy: OnFailure
      containers:
      - name: migrate
        image: ghcr.io/inspiration-particle/utro-api-gateway:0.1.142 # {"$imagepolicy": "flux-system:utr-staging-api-gateway"}
        command: ["/app/api-gateway"]
        args: ["-migrate=true"]
        env:
        - name: LOG_LEVEL
          value: "debug"
        - name: DB_SSL
          value: "true"
        - name: DB_NAME
          value: "utr_staging"
        - name: DB_HOST
          value: "pg-staging-cluster.default.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: utr-staging-migrator.pg-staging-cluster.credentials.postgresql.acid.zalan.do
              key: username
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: utr-staging-migrator.pg-staging-cluster.credentials.postgresql.acid.zalan.do
              key: password
