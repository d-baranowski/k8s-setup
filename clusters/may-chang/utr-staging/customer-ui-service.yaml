apiVersion: v1
kind: Service
metadata:
  name: utr-staging-customer-ui
  namespace: default
  labels:
    app: utr-staging-customer-ui
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: utr-staging-customer-ui
