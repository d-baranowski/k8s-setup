apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: twilio-staging-sid
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: google-secrets
    kind: ClusterSecretStore
  target:
    name: twilio-staging-sid
    creationPolicy: Owner
  data:
    - secretKey: value
      remoteRef:
        key: twilio-staging-sid
        version: latest
