apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: staging-webhook-secret-resend
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: google-secrets
    kind: ClusterSecretStore
  target:
    name: staging-webhook-secret-resend
    creationPolicy: Owner
  data:
    - secretKey: value
      remoteRef:
        key: staging-webhook-secret-resend
        version: latest
