apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: stripe-staging-webhook-secret
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: google-secrets
    kind: ClusterSecretStore
  target:
    name: stripe-staging-webhook-secret
    creationPolicy: Owner
  data:
    - secretKey: value
      remoteRef:
        key: stripe-staging-webhook-secret
        version: latest
