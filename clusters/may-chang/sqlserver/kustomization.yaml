apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - sa-password-secret.yaml
  - platnik-user-secret.yaml
  - platnik-users-secret.yaml
  - backup-secret.yaml
  - backup-aws-creds.yaml
  - statefulset.yaml
  - service.yaml
  - platnik-bootstrap-job.yaml
  # Temporary, for the duration of the migration. Delete this line and the
  # file together once there is a single database again.
  - platnik-migracja-job.yaml
  - backup-cronjob.yaml
  - alerts.yaml
