apiVersion: v1
kind: Service
metadata:
  name: utro-customer-ui
  namespace: default
  labels:
    app: utro-customer-ui
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: utro-customer-ui
