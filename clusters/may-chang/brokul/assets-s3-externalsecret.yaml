# GENERATED by brokul/infra/terraform — DO NOT EDIT
#
# Edits here are silently reverted on the next `terraform apply`: local_file
# has no drift detection, so Terraform neither notices nor reports a change.
# Change assets-secrets.tf instead.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: brokul-assets-s3
  namespace: brokul
spec:
  refreshInterval: 1h
  secretStoreRef:
    # The cluster has exactly ONE store and it is called google-secrets.
    # There is no gcp-secret-manager: an ExternalSecret naming a store that
    # does not exist is not rejected, it just never syncs — and the pod waits
    # on a Secret that is never going to appear.
    name: google-secrets
    kind: ClusterSecretStore
  target:
    name: brokul-assets-s3
    creationPolicy: Owner
  data:
    - secretKey: STORAGE_ACCESS_KEY
      remoteRef:
        key: brokul-assets-s3-access-key-id
    - secretKey: STORAGE_SECRET_KEY
      remoteRef:
        key: brokul-assets-s3-secret-access-key
