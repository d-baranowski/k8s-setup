apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No top-level `namespace:` field: HelmRepositories must live in flux-system
# (where the helm-controller resolves `sourceRef`), while everything else
# already declares `metadata.namespace: observability` explicitly. A global
# kustomize-level namespace would override the flux-system one and break the
# HelmReleases.

# This directory only holds CRD-safe resources (namespace, ExternalSecrets,
# HelmRepository/HelmRelease, Certificate, ClusterRole/CronJob, etc.) — every
# kind here already has its CRD on the cluster. The Grafana/GrafanaDatasource/
# GrafanaDashboard CRs live in clusters/may-chang/observability-resources/ and
# are reconciled by a separate Flux Kustomization with dependsOn: observability
# so the root sweep never trips on missing operator CRDs.
resources:
  - namespace.yaml
  # External Secrets (S3 creds only).
  # Grafana admin credentials are NOT managed via ExternalSecret because the
  # grafana-operator insists on owning a Secret named `<grafana>-admin-credentials`
  # to store the password it uses for its own API calls to Grafana. Letting ESO
  # own that name caused the operator's "admin user" stage to fail. Read the
  # operator-generated password with:
  #   kubectl -n observability get secret grafana-admin-credentials \
  #     -o jsonpath='{.data.GF_SECURITY_ADMIN_PASSWORD}' | base64 -d
  - loki-s3-credentials.yaml
  - tempo-s3-credentials.yaml
  # Helm repos
  - helmrepo-prometheus-community.yaml
  - helmrepo-grafana.yaml
  - helmrepo-grafana-operator.yaml
  - helmrepo-opentelemetry.yaml
  # Helm releases
  - helmrelease-kube-prometheus-stack.yaml
  - helmrelease-loki.yaml
  - helmrelease-promtail.yaml
  - helmrelease-tempo.yaml
  - helmrelease-grafana-operator.yaml
  # OTel Collector fans out app-pushed OTLP into traces→Tempo and
  # metrics→Prometheus remote_write. Tempo's OTLP endpoint only handles
  # traces (returns 404 on /v1/metrics) so apps that emit both must push
  # to the collector, not directly to Tempo.
  - helmrelease-otel-collector.yaml
  # The Tailscale exposure (nginx-proxy + cert-sync CronJob) lives in
  # clusters/may-chang/observability-resources/ alongside the Grafana CR.
  # nginx's upstream is the Service created by the operator from the Grafana
  # CR; keeping them in the same Flux Kustomization avoids a deadlock where
  # the nginx Deployment health-check fails (no Service to resolve) and
  # stalls the entire `observability` Kustomization, which would in turn
  # block `observability-resources` (the very Kustomization that applies
  # the Grafana CR that creates the Service).
