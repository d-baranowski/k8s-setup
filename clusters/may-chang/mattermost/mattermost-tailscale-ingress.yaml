apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mattermost-tailscale
  namespace: default
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: mattermost
      port:
        number: 8065
  tls:
    - hosts:
        - mattermost
